/** 
 * Configuration area sections enum. 
 */
export enum Section {
  ALBUM_FAVORITES = 'albumfavorites',
  ARTIST_FAVORITES = 'artistfavorites',
  AUDIOBOOK_FAVORITES = 'audiobookfavorites',
  CATEGORYS = 'categorys',
  DEVICES = 'devices',
  EPISODE_FAVORITES = 'episodefavorites',
  PLAYER = 'player',
  PLAYLIST_FAVORITES = 'playlistfavorites',
  RECENTS = 'recents',
  SEARCH_MEDIA = 'searchmedia',
  SHOW_FAVORITES = 'showfavorites',
  TRACK_FAVORITES = 'trackfavorites',
  USERPRESETS = 'userpresets',
  /* the following are used to denote card configuration / setup issues. */
  UNDEFINED = 'undefined',
  //INITIAL_CONFIG = 'initialconfig',
}
